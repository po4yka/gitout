"""gitout — Python port of the Kotlin git backup tool.

The Kotlin implementation under ``src/main/kotlin`` remains the reference during
the migration. Modules here are ported one subsystem at a time, each gated by the
characterization suite in ``tests/`` (behavior captured from the Kotlin code).
"""

__version__ = "0.4.0-fork-dev"
